# Lokal geliştirme (SQLite) için sade bağımlılıklar.
# PostgreSQL sürücüleri (asyncpg, psycopg2-binary) ÇIKARILDI — Windows'ta sık derleme
# hatası verir ve SQLite için gerekli değildir. Alembic de yok (init_db.py kullanılır).
fastapi==0.115.0
uvicorn[standard]==0.30.0
sqlalchemy==2.0.32
pydantic==2.7.4
pydantic-settings==2.3.0
PyJWT==2.8.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.9
redis==5.0.1
email-validator==2.1.1
aiosqlite==0.20.0
fakeredis==2.23.2
httpx==0.27.0
